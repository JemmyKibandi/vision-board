specVersion: "v1beta"
serviceId: "vision-board"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vision-board-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
